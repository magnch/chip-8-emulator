[package]
name = "chip8-core"
version = "0.3.0"
edition = "2024"

[dependencies]
rand = "0.10.2"

[features]
# Exposes CpuState, Memory, Instruction, decode(), and Chip8's state-inspection/mutation
# methods (get_state, get_memory_content, set_register, set_pc) for a debugger frontend.
debug-tools = []
